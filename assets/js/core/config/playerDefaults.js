export const PLAYER_DEFAULTS = {
    // Playback
    autoplay: false,
    loop: false,
    muted: false,
    volume: 1.0,
    playbackRate: 1.0,

    // UI
    theme: 'dark',
    showControls: true,
    mode: 'player',

    // Advanced
    bufferSize: 30,
    maxRetries: 3,
};
